
@using Pustok_Temp.Areas.PustokAdmin.ViewModels
@model CreateBookVM
<div class="container">
    <h1>Add Books</h1>
    <form method="post" enctype="multipart/form-data">
        <div class="mb-3">
            <label asp-for="Title" class="form-label">Title</label>
            <input asp-for="Title" type="text" class="form-control">
            <span asp-validation-for="Title" style="color:red"></span>
        </div>
        <div class="mb-3 col-md-3">
            <label asp-for="ImageFile" class="form-label">Image File</label>
            <input asp-for="ImageFile" class="form-control">
            <span asp-validation-for="ImageFile" style="color:red"></span>
        </div>
        <div class="mb-3">
            <label asp-for="Price" class="form-label">Price</label>
            <input asp-for="Price" type="text" class="form-control">
            <span asp-validation-for="Price" style="color:red"></span>
        </div>
        <div class="mb-3">
            <label asp-for="CategoryId">Categories</label>
            <select class="form-select" asp-for="CategoryId" asp-items="new SelectList(ViewBag.Categories,nameof(Category.Id), nameof(Category.Name))">
                <option selected> Browse All Categories...</option>

            </select>
        </div>
        <div class="mb-3">
            <label asp-for="AuthorId">Authors</label>
            <select class="form-select" asp-for="AuthorId" asp-items="new SelectList(ViewBag.Authors,nameof(Author.Id), nameof(Author.Name))">
          <option selected>Browse All Authors...</option></a>

            </select>
        </div>
        <div class="mb-3">
            <label asp-for="AuthorId">Authors</label>
            <select class="multiple form-select" asp-for="AuthorId" asp-items="new SelectList(ViewBag.tags,nameof(Tag.Id), nameof(Tag.Name))">
                <option selected>Browse All Tags...</option></a>

            </select>
        </div>

        <button type="submit" class="btn btn-primary">Submit</button>
    </form>
</div>